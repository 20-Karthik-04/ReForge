/**
 * @file App.jsx
 * @description Root application component. Minimal shell — no pages, no routing,
 * no demo content. Phase 11 and beyond will populate this component.
 */

/**
 * App – root component for the ReForge application.
 *
 * @returns {JSX.Element} Root div mount point.
 */
function App() {
  return <div id="app" />;
}

export default App;
